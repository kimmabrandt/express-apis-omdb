<div>
	<h2>Search Results:</h2>
</div>
<div>
<!-- 	<% movies.forEach(function(movie){ %>
	<div>
		<h3> <%= movie.Title %> (<%= movie.Year %>) </h3>
		<img src="<%= movie.Poster %>">
	</div>
	<% }); %>  -->
</div>
